import { Router } from "express";

const coreRouter = Router();

// TODO: Move health checks and session management routes here
// - Health check endpoint (/api/health)
// - Session validation
// - Authentication status

export default coreRouter;